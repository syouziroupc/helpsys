# HelpSys Education

HelpSys Education は、通常版 HelpSys とは別の Windows アプリです。

- **通常版 HelpSys**: 現在のPC状態から、目的達成のための正しい次の1手を案内する。
- **HelpSys Education**: PC未経験者が0から日常利用に必要な基礎を体系的に学ぶ。

別実行ファイル `HelpSys.Education.exe`、別preview release、別教育AI Workerとして運用します。

## 基本方針

学習は `教育 → 練習 → テスト → 合格 → 次の単元` の順です。

教育版v2では自己申告だけの合格をやめ、次を必須にします。

1. 教育画面で意味と安全上の注意を理解する。
2. 練習画面で実際の操作またはチェック課題を完了する。
3. テストで確認問題に **100%正解** する。
4. 間違えた場合は何度でも復習・再受験する。

テスト中は教育AIを呼びません。答えをAIから取得する経路そのものを設けません。

## 0からの基礎課程

### 0. はじめの一歩
- パソコン・アプリ・ファイル・フォルダーとは何か
- 電源、スリープ、再起動、シャットダウン
- 分からない警告・支払い・削除・権限要求では止まる

### 1. 入力操作
- マウスの移動、1回押し、素早い2回押し、スクロール
- Enter、Backspace、Shift、矢印、Ctrl、Windowsキー
- 文字入力、カーソル、修正
- 日本語入力、IME、変換、確定
- タイピングと正確さ

### 2. Windows
- デスクトップ、タスクバー、スタート
- 最小化、最大化、ウィンドウ切り替え
- アプリの起動・終了、Windows設定

### 3. ファイル管理
- ファイル、フォルダー、保存場所、エクスプローラー
- 保存、開く、名前を付けて保存
- コピー、移動、名前変更、削除、ごみ箱
- ダウンロード、ZIP、USBメモリ

### 4. インターネット
- ブラウザ、検索、タブ、戻る、公式サイト確認
- Webフォーム、アップロード、ダウンロード
- Wi-Fi、ルーター、インターネット接続の切り分け

### 5. 安全
- アカウント、パスワード、多要素認証
- フィッシング、偽警告、サポート詐欺
- カメラ・マイク等の権限、管理者権限、Windows Update

### 6. 日常作業
- メール、返信、全員返信、添付、CC/BCC
- 文書編集、選択、コピー、貼り付け、元に戻す
- PDF、印刷、スクリーンショット

### 7. 自力で対処
- 固まった・遅い・動かないときの切り分け
- バックアップ、クラウド、空き容量
- オンライン会議、マイク、カメラ、画面共有

### 8. 総合
- アプリ起動 → 入力 → 保存 → Web検索 → ファイル確認 → 安全に終了、を一連で行う

## 内部練習

外部教材だけに依存せず、基礎操作はHelpSys Education内でも練習します。

- マウス: 1回押し、素早い2回押し、スクロールを自動判定
- キーボード: Enter / Backspace / Shift / 矢印キーを自動判定
- 文字入力: 見本と完全一致したか自動判定
- その他: 実習手順をすべて確認するチェック式練習
- タイピング: 内部入力課題 + e-typing の外部反復教材

将来、通常版HelpSysの認識・操作検証エンジンを共有し、実際のWindows画面で行う課題も自動採点へ移します。

## 進捗・習熟

`LocalApplicationData/HelpSys.Education/progress.json` に保存します。

単元ごとに以下を記録します。

- 教育完了
- 練習完了
- テスト合格
- テスト受験回数
- 最高得点
- AIヒント利用回数
- 各段階の初回完了日時

起動時は未合格の最初の単元へ自動復帰します。アプリ内の **全体進捗** で課程全体を確認できます。

## 用語集

アプリ内に検索可能な用語集を持ちます。アプリ、ファイル、フォルダー、URL、ドメイン、Wi-Fi、アップロード、バックアップ、CC/BCCなど、初心者がつまずきやすい用語を平易な文章で確認できます。

## 教育AI

通常版HelpSysとは別の `helpsys-education` Workerです。

- `education`: 初心者向け説明
- `practice`: 3段階ヒント
- `test`: AIを呼ばない
- 秘密情報をHelpSysへ入力させる出力は最終ガードで拒否
- モデルは `HELPSYS_EDUCATION_MODEL` で通常版と独立して交換可能

ローカル:

```powershell
npm run test:education
npm run dev:education
```

Cloudflareへ別Workerとしてデプロイ:

```powershell
npm run deploy:education
```

デスクトップ側でAIヒントを使う場合のみ:

```powershell
$env:HELPSYS_EDUCATION_API_BASE="https://<education-worker>"
```

APIキーを使う場合はWorker側とWindows側の `HELPSYS_EDUCATION_API_KEY` を合わせます。

## CI

Education smokeでは以下を自動確認します。

- 教育AI Workerのself-test
- Windows Releaseビルド
- `--self-test` によるカリキュラム整合性検査
- 単元ID重複、学習目標、練習項目、確認問題、用語集の最低件数
- 実際のEducation UI起動
- 全体進捗・用語集・教育/練習/テスト画面のUIA検出
- 練習未完了状態ではテスト開始がブロックされること

## 次の拡張

- 通常版の画面認識・操作検証エンジンを共有ライブラリ化
- ファイル操作、ブラウザ操作、Windows操作の実技自動採点
- 苦手単元の自動復習キュー
- 一定期間後の再テストによる定着確認
- 学習履歴から次に復習すべき単元を選ぶ適応学習
