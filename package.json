{
  "name": "helpsys",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check worker/index.js && node --check worker/deliberation-guard.js",
    "test": "node worker/selftest.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
