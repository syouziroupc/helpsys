<Window x:Class="HelpSys.Stable.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HelpSys Stable"
        Width="520"
        Height="310"
        MinWidth="460"
        MinHeight="270"
        WindowStartupLocation="CenterScreen">
  <Grid Margin="22">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0">
      <TextBlock Text="HelpSys Stable"
                 FontSize="25"
                 FontWeight="Bold" />
      <TextBlock Margin="0,5,0,0"
                 Foreground="#FF4B5563"
                 Text="Gemini 3.8 Flash 専用 / 操作は実行せず、次の1手だけ案内します。" />
    </StackPanel>

    <StackPanel Grid.Row="1" Margin="0,20,0,0">
      <TextBlock Text="やりたいこと" FontWeight="SemiBold" />
      <TextBox x:Name="GoalBox"
               Margin="0,7,0,0"
               Height="64"
               Padding="10,8"
               FontSize="16"
               AcceptsReturn="True"
               TextWrapping="Wrap"
               VerticalScrollBarVisibility="Auto"
               Text="メモ帳を開きたい" />
    </StackPanel>

    <Border Grid.Row="2"
            Margin="0,18,0,0"
            Padding="12"
            Background="#FFF3F4F6"
            CornerRadius="8">
      <TextBlock x:Name="StatusText"
                 Text="待機中。操作したい画面を前面に出して F8 を押してください。"
                 TextWrapping="Wrap"
                 VerticalAlignment="Center" />
    </Border>

    <Grid Grid.Row="3" Margin="0,18,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Column="0"
                 VerticalAlignment="Center"
                 Foreground="#FF6B7280"
                 Text="ショートカット: F8" />
      <Button x:Name="CancelButton"
              Grid.Column="1"
              Width="90"
              Height="38"
              Margin="0,0,10,0"
              Content="中止"
              Click="CancelButton_Click" />
      <Button x:Name="GuideButton"
              Grid.Column="2"
              Width="130"
              Height="38"
              FontWeight="SemiBold"
              Content="案内する  F8"
              Click="GuideButton_Click" />
    </Grid>
  </Grid>
</Window>
